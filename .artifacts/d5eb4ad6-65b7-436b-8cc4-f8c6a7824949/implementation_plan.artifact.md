# Implementation Plan: Enhancing Project Coherence and Connectivity

This plan addresses the gaps identified during the project analysis, specifically the missing backend integration and the transition from mock AI features to live functionality.

## User Review Required

> [!IMPORTANT]
> The project currently lacks the FastAPI backend and Llama 3 integration specified in `AGENTS.md`. I will initialize a basic FastAPI structure and wire up the Flutter frontend to communicate with it.

## Proposed Changes

### [Backend Infrastructure]

#### [NEW] [main.py](file:///C:/AndroidStudioProjects/checkmate/backend/main.py)
- Initialize FastAPI app.
- Define endpoints for:
    - `/generate-exam`: Llama 3 powered exam generation.
    - `/analyze-results`: Pedagogical analysis of graded OMR data.
    - `/save-grades`: Storing OMR results.

#### [NEW] [requirements.txt](file:///C:/AndroidStudioProjects/checkmate/backend/requirements.txt)
- Add `fastapi`, `uvicorn`, `pydantic`, and `groq` (or preferred LLM client for Llama 3).

### [Frontend Services]

#### [NEW] [api_service.dart](file:///C:/AndroidStudioProjects/checkmate/lib/services/api_service.dart)
- Implement `ApiService` using `Dio`.
- Add methods for `generateExam`, `analyzeResults`, and `syncCourseData`.

#### [MODIFY] [pubspec.yaml](file:///C:/AndroidStudioProjects/checkmate/pubspec.yaml)
- Ensure all necessary dependencies are up to date.

### [UI Logic Updates]

#### [MODIFY] [ai_questionnaire_screen.dart](file:///C:/AndroidStudioProjects/checkmate/lib/screens/ai_questionnaire_screen.dart)
- Replace mock `_startGeneration` with a call to `ApiService.generateExam`.

#### [MODIFY] [ai_analysis_screen.dart](file:///C:/AndroidStudioProjects/checkmate/lib/screens/ai_analysis_screen.dart)
- Update "FINISH SESSION" to upload the `ProcessedSheet` data via `ApiService`.

## Verification Plan

### Automated Tests
- Unit tests for `ApiService` using mock adapters.
- Integration tests for FastAPI endpoints using `pytest`.

### Manual Verification
- Verify that a topic entered in `AIQuestionnaireScreen` returns questions generated by the backend.
- Confirm that graded OMR results appear in the "Academic Overview" after syncing.
